name: localbill_server
description: "Lightweight sync server for the LocalBill Flutter app."
publish_to: none

version: 1.0.0

environment:
  sdk: ^3.7.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
